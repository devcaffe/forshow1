[program:{{ proj_name }}]

command = {{ proj_path }}/gunicorn_start.sh
user = {{ user }}
startretries=2
autostart=true
autorestart=true
stopwaitsecs = 60
# stdout_logfile = {{ proj_logdir }}/{{ proj_name }}.log
# stderr_logfile = {{ proj_logdir }}/{{ proj_name }}.error.log
